import React from 'react'

const Searchbar = ({className , array}) => {
  return (
    <div className={` relative   border rounded-[100px]    cursor-pointer   ${className}`}>
      <div></div>
    </div>  
  )
}

export default Searchbar
